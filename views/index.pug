extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 Bonjour!
      h1 Avez-vous un projet web en tête?
      h1 Créons le ensemble!
      h1 Le ciel est la limite :)

      p.lead.text-light
        | Web apps, REST APIs, platformes e-commerce...

      .grid-x
        .cell.medium-6 
          a.button.secondary(href="/fr/about") Recrutez-moi
          a.button(href="/static/CV.pdf", target='_blank') Curriculum Vitae
  
  article.grid-container.portfolio-index
    h2.projects Mes créations
    hr 
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      each val, index in render
        .cell
          a(href="/fr/project/" + render[index][0])
            img.thumbnail(src="/static" + render[index][2])
            h5= render[index][1]
      
